/** @format */

import React, { SFC } from 'react';
import DashboardLayout from '../layouts/DashboardLayout';

const MainPage: SFC = () => {
  return (
    <DashboardLayout brandText="DASHBOARD" brandGradient="bg-gradient-default">
      dashboard
    </DashboardLayout>
  );
};

export default MainPage;
